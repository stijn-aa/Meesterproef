{
  "name": "meesterproef",
  "version": "0.0.1",
  "description": "meesterproef",
  "main": "testserver.js",
  "scripts": {
    "start": "nodemon testserver.js",
    "deploy": "git add . && git commit -am \"make it better\" && git push heroku master && heroku logs --tail",
    "push": "git add . && git commit -am \"make it better\" && git push origin master"
  },
  "dependencies": {
    "actions-on-google": "^2.6.0",
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "fs": "0.0.1-security",
    "nodemon": "^1.18.11"
  }
}
